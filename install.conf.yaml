- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.gitconfig: gitconfig
    ~/.zprofile: zprofile
    ~/.nvm: nvm
    ~/.zshrc: zshrc

- shell:
  - [git submodule update --init --recursive, Installing submodules]